scanners:
  - "python"
issue_trackers:
  - kind: "github"
    authentication:
      env: "GITHUB_TOKEN"
  - kind: "github"
    host: "local.github"
    authentication:
      env: "LOCAL_GITHUB_TOKEN"
  - kind: "gitlab"
    authentication:
      env: "GITLAB_TOKEN"
